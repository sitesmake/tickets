<%= form_for([:admin, @railway_station]) do |f| %>
  <%= render 'shared/errors', resource: @railway_station %>

  <div class="field">
    <%= f.label :title %><br>
    <%= f.text_field :title %>
  </div>
  <div class="actions">
    <%= f.submit t('.submit')%>
  </div>
<% end %>
